name: Rebuild Movie Similarity Matrix

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Rebuild similarity matrix
        run: |
          node scripts/rebuild-similarity-matrix.js

      - name: Validate matrix output
        run: |
          test -f public/similarity_matrix.json
          node -e "const matrix=require('./public/similarity_matrix.json'); console.log('Matrix entries:', Object.keys(matrix).length)"

      - name: Upload matrix artifact
        uses: actions/upload-artifact@v4
        with:
          name: movie_similarity
          path: public/similarity_matrix.json
